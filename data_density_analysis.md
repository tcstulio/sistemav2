# Data Density Analysis Report
Generated at: 2025-12-13T02:03:51.831Z

Comparing manual TypeScript interfaces against full Swagger definitions to identify missing data fields.

### ⚠️ Interface `ThirdParty`: No matching Swagger definition found.
## Entity: Invoice (Swagger: `updateInvoicesModel`)
- **Coverage**: We use **13** out of **1** available fields (1300%).
- **Top Missing Data** (first 20):
  `request_data`
- **Potential Type Conflicts**:
  *(Requires deeper AST analysis for strict checking, mostly looks ok)*

## Entity: Product (Swagger: `updateProductsModel`)
- **Coverage**: We use **9** out of **1** available fields (900%).
- **Top Missing Data** (first 20):
  `request_data`
- **Potential Type Conflicts**:
  *(Requires deeper AST analysis for strict checking, mostly looks ok)*

## Entity: Project (Swagger: `updateProjectsModel`)
- **Coverage**: We use **9** out of **1** available fields (900%).
- **Top Missing Data** (first 20):
  `request_data`
- **Potential Type Conflicts**:
  *(Requires deeper AST analysis for strict checking, mostly looks ok)*

## Entity: Task (Swagger: `updateTasksModel`)
- **Coverage**: We use **13** out of **1** available fields (1300%).
- **Top Missing Data** (first 20):
  `request_data`
- **Potential Type Conflicts**:
  *(Requires deeper AST analysis for strict checking, mostly looks ok)*

## Entity: Proposal (Swagger: `updateProposalsModel`)
- **Coverage**: We use **9** out of **1** available fields (900%).
- **Top Missing Data** (first 20):
  `request_data`
- **Potential Type Conflicts**:
  *(Requires deeper AST analysis for strict checking, mostly looks ok)*

## Entity: Order (Swagger: `updateSupplierordersModel`)
- **Coverage**: We use **9** out of **1** available fields (900%).
- **Top Missing Data** (first 20):
  `request_data`
- **Potential Type Conflicts**:
  *(Requires deeper AST analysis for strict checking, mostly looks ok)*

### ⚠️ Interface `User`: Could not parse manual definition in types.ts.
## Entity: BankAccount (Swagger: `createBankaccountsModel`)
- **Coverage**: We use **9** out of **1** available fields (900%).
- **Top Missing Data** (first 20):
  `request_data`
- **Potential Type Conflicts**:
  *(Requires deeper AST analysis for strict checking, mostly looks ok)*
